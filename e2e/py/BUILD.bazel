load("@rules_bound//bound:defs.bzl", "bound_binary")
load("@rules_python//python:py_binary.bzl", "py_binary")

py_binary(
    name = "report",
    srcs = ["report.py"],
    data = ["data.json"],
    deps = ["@rules_python//python/runfiles"],
)

bound_binary(
    name = "report_bound",
    binary = ":report",
    bound_env = {"REPORT_TITLE": "Bound report"},
    visibility = ["//visibility:public"],
)
